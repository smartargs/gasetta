# apps/web/.env.example — Copy to .env.local and fill in. NEVER commit .env*.
# Vite only exposes vars prefixed with VITE_ to the browser.

# Public-by-design — these *do* ship in the bundle. Safety comes from RLS.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
